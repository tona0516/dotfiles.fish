# Note: コマンドラインツールは基本的にmiseで導入する

brew "fish"

if OS.mac?
    # iOS
    brew "cocoapods"
    brew "carthage"
    brew "fastlane"

    # AI
    cask "claude-code"
    cask "copilot-cli"
    cask "codex"

    # その他
    cask "iterm2"
    cask "rectangle"
    cask "visual-studio-code"
    cask "font-hack-nerd-font"
end

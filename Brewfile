# Note: コマンドラインツールは基本的にmiseで導入する

brew "fish"

if OS.mac?
    # PW要求
    cask "google-japanese-ime"

    # iOS
    brew "cocoapods"
    brew "carthage"
    brew "fastlane"

    # その他
    cask "copilot-cli"
    cask "rectangle"
    cask "visual-studio-code"
end

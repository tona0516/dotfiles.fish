# Note: コマンドラインツールは基本的にDevboxで導入する

if OS.mac?
    # PW要求
    cask "docker"
    cask "google-japanese-ime"
    cask "zoom"

    # iOS
    brew "cocoapods"
    brew "carthage"
    brew "fastlane"

    # その他
    cask "rectangle"
    cask "visual-studio-code"
end